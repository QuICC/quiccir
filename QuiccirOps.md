<!-- Autogenerated by mlir-tblgen; don't manually edit -->
### `quiccir.add` (quiccir::AddOp)

_AddOp_


Syntax:

```
operation ::= `quiccir.add` $lhs `,` $rhs `:` type($rhs) `,` type($lhs) `->` type($res) attr-dict-with-keyword
```

The `quiccir.add` operation implements a pointwise addition.
?variadic?

Example:

```mlir
%res = quiccir.add %lhs, %rhs: tensor<?x?x?x?>, tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait, Commutative, Elementwise, SameOperandsAndResultElementType, SameOperandsAndResultShape

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | tensor of any type values
| `rhs` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `res` | tensor of any type values

### `quiccir.al.int` (quiccir::AlIOp)

_AssociateLegendre Transform from physical to modal space_


Syntax:

```
operation ::= `quiccir.al.int` $phys `:` type($phys)  `->` type($mods) attr-dict-with-keyword
```

The `quiccir.al.int` operation implements a generic AssociateLegendre integrator.
At the momement there is no implemented path for code generation,
but only lowering to library calls.

This operation takes one tensor representing, the value at the
integration points, it returns a tensor representing the
modes values.

Attributes:
- implptr, index to retrieve backend class implementation
- dim, size of the operator that cannot be inferred from the input

Example:

```mlir
%mods = quiccir.al.int<implptr = 0 : i64 : !llvm.ptr, dim = 3 : i64> %phys: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface, TransformOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `dim` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `phys` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `mods` | tensor of any type values

### `quiccir.al.prj` (quiccir::AlPOp)

_AssociateLegendre Transform modal to physical space_


Syntax:

```
operation ::= `quiccir.al.prj` $mods `:` type($mods)  `->` type($phys) attr-dict-with-keyword
```

The `quiccir.al.prj` operation implements a generic AssociateLegendre projector.

This operation takes one tensor representing, the
modes values, it returns a tensor representing the value at the
integration points.

Attributes:
- implptr, index to retrieve backend class implementation
- dim, size of the operator that cannot be inferred from the input

Example:

```mlir
%phys = quiccir.al.prj<implptr = 0 : i64 : !llvm.ptr, dim = 3 : i64> %mods: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface, TransformOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `dim` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `mods` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `phys` | tensor of any type values

### `quiccir.alloc_data` (quiccir::AllocDataOp)

_Allocate memref data for a view_


Syntax:

```
operation ::= `quiccir.alloc_data` `(` $ptr `,` $idx `)` `,` $lds `:` `(`type($ptr) `,` type($idx) `)` `,` type($lds) `->` type($data) attr-dict
```

The `quiccir.alloc_data` operation allocate a 1D memref that is
the buffer for the compressed data of a view.

In order to allocate the right amount of space for the data
we need to know view layout, column height (possibly padded for FFT)
and the pointers/indexes metadata.

Example:

```mlir
%0 = quiccir.alloc_data(%ptr, %idx), %lds : (memref<xi32>, memref<xi32>), i64 ->
    memref<?xf64>  {layout="layout"}
```

For codegen we should lower this to a memref alloc of appropriate size


#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `layout` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `ptr` | memref of any type values
| `idx` | memref of any type values
| `lds` | 64-bit signless integer

#### Results:

| Result | Description |
| :----: | ----------- |
| `data` | memref of any type values

### `quiccir.assemble` (quiccir::AssembleOp)

_Assemble view from meta and data MemRefs_


Syntax:

```
operation ::= `quiccir.assemble` `(` $ptr `,` $idx `)` `,` $data `:` `(`type($ptr) `,` type($idx) `)` `,` type($data) `->` qualified(type($view)) attr-dict
```

The `quiccir.assemble` create a view from its data and meta data.

Example:

```mlir
%0 = quiccir.assemble(%ptr, %idx), %data : (memref<xi32>, memref<xi32>), memref<?xf64> ->
    !quiccir.view<?x?x?xf64, "layout">
```


#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `ptr` | memref of any type values
| `idx` | memref of any type values
| `data` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `view` | view of any type values

### `quiccir.cross` (quiccir::CrossOp)

_Cross product of 2 vectors_


Syntax:

```
operation ::= `quiccir.cross` `(` $V0 `,` $V1 `,` $V2 `)` `,` `(` $U0 `,` $U1 `,` $U2 `)` `:` `(` type($V0) `,` type($V1) `,` type($V2) `)`
              `,` `(` type($U0) `,` type($U1) `,` type($U2) `)`  `->` `(` type($C0) `,` type($C1) `,` type($C2) `)`
              attr-dict-with-keyword
```

The `quiccir.cross` operation implements a cross product of 2 vectors
in component wise form, each component is a tensor.

This operation takes 6 tensors representing the vectors,
it returns 3 tensors the cross product.

Attributes:
- implptr, index to retrieve backend class implementation
- kind, described the constant scaling factor

(The scaling factor should be exposed in Mlir for code generation)

Example:

```mlir
%C0, %C1, %C2 = quiccir.cross (%V0, %V1, %V2), (%U0, %U1, %U2) :
(tensor<?x?x?xf64>, tensor<?x?x?xf64>, tensor<?x?x?xf64>), (tensor<?x?x?xf64>, tensor<?x?x?xf64>, tensor<?x?x?xf64>) ->
(tensor<?x?x?xf64>, tensor<?x?x?xf64>, tensor<?x?x?xf64>)
attributes{implptr = 0, kind = "inertia"}
```

Traits: AlwaysSpeculatableImplTrait, SameOperandsAndResultElementType, SameOperandsAndResultShape, SameOperandsElementType, SameOperandsShape

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface)

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `V0` | tensor of any type values
| `V1` | tensor of any type values
| `V2` | tensor of any type values
| `U0` | tensor of any type values
| `U1` | tensor of any type values
| `U2` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `C0` | tensor of any type values
| `C1` | tensor of any type values
| `C2` | tensor of any type values

### `quiccir.dealloc` (quiccir::DeallocOp)

_Deallocate view_


Syntax:

```
operation ::= `quiccir.dealloc` `(` $view `)` `:` qualified(type($view)) attr-dict
```

The `quiccir.dealloc` operation deallocate a view buffer.

Example:

```mlir
quiccir.dealloc(%arg) : !quiccir.view<?x?x?xf64, "layout">
```

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `view` | view of any type values

### `quiccir.dot` (quiccir::DotOp)

_Dot product of 2 vectors_


Syntax:

```
operation ::= `quiccir.dot` `(` $V0 `,` $V1 `,` $V2 `)` `,` `(` $U0 `,` $U1 `,` $U2 `)` `:` `(` type($V0) `,` type($V1) `,` type($V2) `)`
              `,` `(` type($U0) `,` type($U1) `,` type($U2) `)`  `->` type($dot)
              attr-dict-with-keyword
```

The `quiccir.dot` operation implements a dot product of 2 vectors
in component wise form, each component is a tensor.

This operation takes 6 tensors representing the vectors,
it returns 1 tensors the dot product.

Attributes:
- implptr, index to retrieve backend class implementation
- kind, described the constant scaling factor

(The scaling factor should be exposed in Mlir for code generation)

Example:

```mlir
%dot = quiccir.dot (%V0, %V1, %V2), (%U0, %U1, %U2) :
(tensor<?x?x?xf64>, tensor<?x?x?xf64>, tensor<?x?x?xf64>), (tensor<?x?x?xf64>, tensor<?x?x?xf64>, tensor<?x?x?xf64>) ->
tensor<?x?x?xf64>
attributes{implptr = 0, kind = "inertia"}
```

Traits: AlwaysSpeculatableImplTrait, SameOperandsAndResultElementType, SameOperandsAndResultShape, SameOperandsElementType, SameOperandsShape

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface)

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `V0` | tensor of any type values
| `V1` | tensor of any type values
| `V2` | tensor of any type values
| `U0` | tensor of any type values
| `U1` | tensor of any type values
| `U2` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `dot` | tensor of any type values

### `quiccir.fr.int` (quiccir::FrIOp)

_Fourier Transform from physical to modal space_


Syntax:

```
operation ::= `quiccir.fr.int` $phys `:` type($phys)  `->` type($mods) attr-dict-with-keyword
```

The `quiccir.fr.int` operation implements a generic Fourier integrator.
At the momement there is no implemented path for code generation,
but only lowering to library calls.

This operation takes one tensor representing, the value at the
integration points, it returns a tensor representing the
modes values.

Attributes:
- implptr, index to retrieve backend class implementation
- dim, size of the operator that cannot be inferred from the input

Example:

```mlir
%mods = quiccir.fr.int<implptr = 0 : i64 : !llvm.ptr, dim = 3 : i64> %phys: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface, TransformOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `dim` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `phys` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `mods` | tensor of any type values

### `quiccir.fr.prj` (quiccir::FrPOp)

_Fourier Transform modal to physical space_


Syntax:

```
operation ::= `quiccir.fr.prj` $mods `:` type($mods)  `->` type($phys) attr-dict-with-keyword
```

The `quiccir.fr.prj` operation implements a generic Fourier projector.

This operation takes one tensor representing, the
modes values, it returns a tensor representing the value at the
integration points.

Attributes:
- implptr, index to retrieve backend class implementation
- dim, size of the operator that cannot be inferred from the input

Example:

```mlir
%phys = quiccir.fr.prj<implptr = 0 : i64 : !llvm.ptr, dim = 3 : i64> %mods: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface, TransformOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `dim` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `mods` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `phys` | tensor of any type values

### `quiccir.indices` (quiccir::IndicesOp)

_Indices view from meta and data MemRefs_


Syntax:

```
operation ::= `quiccir.indices` $view `:` qualified(type($view)) `->` type($idx) attr-dict
```

The `quiccir.indices` extract indices meta data from a view.

Example:

```mlir
%idx = quiccir.indices %view : !quiccir.view<?x?x?xf64, "layout"> -> memref<?xi32>
```

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `view` | view of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `idx` | memref of any type values

### `quiccir.jw.int` (quiccir::JWIOp)

_Jone-Worland Transform from physical to modal space_


Syntax:

```
operation ::= `quiccir.jw.int` $phys `:` type($phys)  `->` type($mods) attr-dict-with-keyword
```

The `quiccir.jw.int` operation implements a generic Jones Worland integrator.
At the momement there is no implemented path for code generation,
but only lowering to library calls.
Future codegen path will lower to LinAlg on sparse tensors for
quadrature ops.

This operation takes one tensor representing, the value at the
integration points, it returns a tensor representing the
modes values.

Attributes:
- implptr, index to retrieve backend class implementation
- dim, size of the operator that cannot be inferred from the input

Example:

```mlir
%mods = quiccir.jw.int<implptr = 0 : i64 : !llvm.ptr, dim = 3 : i64> %phys: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface, TransformOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `dim` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `phys` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `mods` | tensor of any type values

### `quiccir.jw.prj` (quiccir::JWPOp)

_Jone-Worland Transform modal to physical space_


Syntax:

```
operation ::= `quiccir.jw.prj` $mods `:` type($mods)  `->` type($phys) attr-dict-with-keyword
```

The `quiccir.jw.prj` operation implements a generic Jones Worland projector.
At the momement there is no implemented path for code generation,
but only lowering to library calls.
Future codegen path will lower to LinAlg on sparse tensors for
quadrature ops.

This operation takes one tensor representing, the
modes values, it returns a tensor representing the value at the
integration points.

Attributes:
- implptr, index to retrieve backend class implementation
- dim, size of the operator that cannot be inferred from the input

Example:

```mlir
%phys = quiccir.jw.prj<implptr = 0 : i64 : !llvm.ptr, dim = 3 : i64> %mods: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface, TransformOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `dim` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `mods` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `phys` | tensor of any type values

### `quiccir.materialize` (quiccir::MaterializeOp)

_Materialize (sparse) tensor to view_


Syntax:

```
operation ::= `quiccir.materialize` $tensor `in` $view  `:` `(` type($tensor) `,` qualified(type($view)) `)` attr-dict
```

The `quiccir.materialize` operation materializes a tensor into an existing
view buffer.
At the moment this is a dense operation, next should be implemented as sparse.
For codegen we should use the sparse tensor infrastructure.

This operation takes one tensor and a view.
In conjuction with the lowering of the tensor producer, it allows to reuse
an existing buffer.

Example:

```mlir
quiccir.materialize %ret in %uval : (tensor<1x3x2xf64>, !quiccir.view<1x3x2xf64, "layout">)
```

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `tensor` | ranked tensor of any type values
| `view` | view of any type values

### `quiccir.mul.const` (quiccir::MulConstOp)

_MulConstOp_


Syntax:

```
operation ::= `quiccir.mul.const` $rhs `:` type($rhs) `->` type($res) attr-dict-with-keyword
```

The `quiccir.mul.const` operation implements a pointwise multiplication with a constant.
For the moment the constant is hidden in the library call, selected by the kind
attribute, it will need to be exposed for code generation.

Example:

```mlir
%res = quiccir.mul.const %rhs: tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait, Elementwise, SameOperandsAndResultElementType, SameOperandsAndResultShape

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, KindOpInterface, NoMemoryEffect (MemoryEffectOpInterface)

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute
| `kind` | ::mlir::StringAttr | string attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `rhs` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `res` | tensor of any type values

### `quiccir.pointers` (quiccir::PointersOp)

_Pointers view from meta and data MemRefs_


Syntax:

```
operation ::= `quiccir.pointers` $view `:` qualified(type($view)) `->` type($ptr) attr-dict
```

The `quiccir.pointers` extract pointers meta data from a view.

Example:

```mlir
%ptr = quiccir.pointers %view : !quiccir.view<?x?x?xf64, "layout"> -> memref<?xi32>
```

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `view` | view of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `ptr` | memref of any type values

### `quiccir.quadrature` (quiccir::QuadratureOp)

_Batched backward transform kernel_


Syntax:

```
operation ::= `quiccir.quadrature` $oprtr `,` $umods `:`  type($oprtr) `,` type($umods)  `->` type($uphys) attr-dict
```

The `quiccir.quadrature` operation implements a batched quadrature based transform kernel.
At the moment this is a dense operation, next should be implemented as sparse.

This operation takes 2 tensors, one representing the operator the second the
modes/quadrature values, it returns a tensor representing the value at the
integration points or the modes.

The shape of the operator and the input tensor need to fit.

The operation represents oprtr * mods = phys or oprtr * vals = mods.

Example:

```mlir
%uphys = quiccir.quadrature %oprtr, %umods: tensor<?x?x?x?>, tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, NoMemoryEffect (MemoryEffectOpInterface)

Effects: MemoryEffects::Effect{}

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `oprtr` | tensor of any type values
| `umods` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `uphys` | tensor of any type values

### `quiccir.sub` (quiccir::SubOp)

_SubOp_


Syntax:

```
operation ::= `quiccir.sub` $lhs `,` $rhs `:` type($rhs) `,` type($lhs) `->` type($res) attr-dict-with-keyword
```

The `quiccir.sub` operation implements a pointwise subtraction.
?variadic?

Example:

```mlir
%res = quiccir.sub %lhs, %rhs: tensor<?x?x?x?>, tensor<?x?x?x?> -> tensor<?x?x?x?>
```

Traits: AlwaysSpeculatableImplTrait, Elementwise, SameOperandsAndResultElementType, SameOperandsAndResultShape

Interfaces: ConditionallySpeculatable, FoldTensorCastIntoConsumerOpInterface, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | tensor of any type values
| `rhs` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `res` | tensor of any type values

### `quiccir.transpose` (quiccir::TransposeOp)

_Transpose operator_


Syntax:

```
operation ::= `quiccir.transpose` $input `permutation` `=` $permutation `:` type($input) `->` type($output) attr-dict-with-keyword
```

Permutes the dimensions based on permutation.

Example:

```mlir
%out = quiccir.transpose %in permutation [0, 2, 1]: tensor<1x2x3xf32> -> tensor<1x3x2xf32>
```

Traits: AlwaysSpeculatableImplTrait

Interfaces: ConditionallySpeculatable, NoMemoryEffect (MemoryEffectOpInterface), ShapeInferenceOpInterface

Effects: MemoryEffects::Effect{}

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `permutation` | ::mlir::DenseI64ArrayAttr | i64 dense array attribute
| `implptr` | ::mlir::IntegerAttr | 64-bit signless integer attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `input` | tensor of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `output` | tensor of any type values

