<!-- Autogenerated by mlir-tblgen; don't manually edit -->
### `-convert-quiccir-to-call`

_Lower the operations from the quiccir dialect into call opsexternal function call_

### `-convert-quiccir-to-llvm`

_Convert quiccir dialect data ops int LLVM ops_

### `-finalize-quiccir-view`

_Lower View to llvm.struct for func/call/return ops_

### `-lower-quiccir-alloc`

_Lower the alloc and dealloc operations from the quiccir dialect into call ops external function call_

### `-quiccir-set-implptr`

_Set unique implementation pointer_

### `-quiccir-transform-contraction`

_Transforms operators distribute over addition and subtraction.This pass contract the distributed transform ops to reduce the number oftranspose ops..i.e. P(T(a)) + P(T(b)) => P(T(a+b)) where P is a transform and T is a transpose._

### `-quiccir-view-deallocation`

_Adds all required dealloc operations for all allocations in the input program_

This pass implements an algorithm to automatically introduce all required
deallocation operations for all buffers in the input program. This ensures
that the resulting program does not have any memory leaks.
### `-quiccir-view-wrapper`

_Insert view wrapper_


#### Options
```
-dim-args : Set Dimensions for arguments
-dim-rets : Set Dimensions for returns
-lay-args : Set View Layout for arguments
-lay-rets : Set View Layout for returns
```
### `-set-quiccir-dims`

_Set View layout based on options and ops_


#### Options
```
-phys : Set Dimensions for physical space
-mods : Set Dimensions for modal space
```
### `-set-quiccir-view-lay`

_Set View layout based on options and ops_


#### Options
```
-lay-ppp2mpp : Set View Layout pair for first integrator
-lay-pmp2mmp : Set View Layout pair for second integrator
-lay-pmm2mmm : Set View Layout pair for second integrator
```
