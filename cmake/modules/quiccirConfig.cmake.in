# Deps
set(MLIR_VERSION @MLIR_VERSION_MAJOR@.@MLIR_VERSION_MINOR@.@MLIR_VERSION_PATCH@)
find_package(MLIR ${MLIR_VERSION} EXACT REQUIRED CONFIG)

# Targets
include("${CMAKE_CURRENT_LIST_DIR}/quiccirTargets.cmake")
